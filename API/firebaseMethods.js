import * as firebase from "firebase";
import 'firebase/firestore';
import {Alert} from 'react-native';

export async function obtenerCursos(){
    try{
        const 
    }catch{
        Alert.alert('Error en cargar datos',err.message);
    }
}